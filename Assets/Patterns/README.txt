# Patterns : Object Pool

Simple object pool system

## How to use

Instead Monobehaviour.Instantiate(...) call ObjectPool.Instantiate(...).
Instead Monobehaviour.Destroy(object) call ObjectPool.Destroy(object).
That's it!